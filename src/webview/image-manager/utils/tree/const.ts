export enum UpdateOrigin {
  image = 'image',
  dir = 'dir',
}

export enum UpdateEvent {
  update = 'update',
  create = 'create',
  delete = 'delete',
}

export enum UpdateType {
  full = 'full',
  patch = 'patch',
}
