export enum Theme {
  dark = 'dark',
  light = 'light',
  auto = 'auto',
}

export enum Language {
  en = 'en',
  zh_CN = 'zh-CN',
  zh_TW = 'zh-TW',
  ja = 'ja',
  auto = 'auto',
}

export enum ReduceMotion {
  auto = 'auto',
  on = 'on',
  off = 'off',
}
